# Generated for {{ .owner.name }}
# Replace the private-key placeholder before use.
[Interface]
PrivateKey = REPLACE_WITH_PRIVATE_KEY
Address = {{ .owner.address }}
{{ range .peers }}{{ if ne .name $.owner.name }}

# {{ .name }}
[Peer]
PublicKey = {{ .publicKey | safe.HTML }}
AllowedIPs = {{ .address }}
{{ with .endpoint }}Endpoint = {{ . }}
{{ end }}
{{ end }}{{ end -}}
